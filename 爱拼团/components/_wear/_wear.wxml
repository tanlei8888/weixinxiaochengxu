<view class="wear">
  <text>{{floorData[0].floor_title.name}}</text>
  <view class="wear-img">
  <block wx:for="{{floorData}}" wx:key="image_src">
   <view class="imgBox">
     <image  mode="aspectFill" src="{{item.image_src}}"></image>
   </view>
  </block>
  </view>
</view>